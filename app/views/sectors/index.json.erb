{
	"sectors":[
	<% # List all sectors %>
<% @sectors.each do |sector| %>	{
		"name": "<%= sector.name %>",
		"category": "<%= sector.category %>",
		"url": "<%= url_for sector %>"
	}<% unless sector == @sectors.last %>,<% end %>
	<% end %>
	]
}